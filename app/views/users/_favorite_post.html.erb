<div class="col">
  <div class="card" style="width: 21rem;" id="favorite_post-<%= favorite_post.id %>">
    <div class="card-img-top">
      <% if favorite_post.images.attached? %>
        <%= image_tag favorite_post.display_image, class: "img-fluid" %>
      <% else %>
        <img src="..." class="" alt="...">
      <% end %>
    </div>
    <div class="card-body">
      <div class="card-title">
        <%= link_to favorite_post.name, favorite_post, class: "h5 text-dark", style: "text-decoration: none" %>
      </div>
      <div class="pt-3 row">
        <div class="col-2">
          <%= link_to gravatar_for(favorite_post.user, size: 50), favorite_post.user %>
        </div>
        <div class="col">
          <%= link_to favorite_post.user.name, favorite_post.user %>
          <p class="text-secondary"><%= time_ago_in_words(favorite_post.created_at) %>前</p>
        </div>
      </div>
    </div>
  </div>
</div>
