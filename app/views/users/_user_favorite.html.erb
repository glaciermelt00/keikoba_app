<% if @user.favorites.any?%>
  <h3>お気に入り (<%= @user.favorites.count %>件)</h3>
  <div class="row row-cols-1 row-cols-md-2 g-4">
    <%= render @favorites %>
  </div>
  <div class="mt-4">
    <%== pagy_bootstrap_nav(@pagy) %>
  </div>
<% end %>